import { Abi } from '@polkadot/api-contract';

declare enum MetadataError {
    InvalidFile = "Invalid file.",
    EmptyWasm = "Wasm field not found.",
    InvalidWasm = "Invalid Wasm field."
}
interface Validation {
    error?: MetadataError;
}
declare const validateMetadata: (metadata: Abi | undefined, { requireWasm }: MetadataOptions) => Validation;

interface MetadataOptions {
    requireWasm?: boolean;
}
interface DeriveMetadataOptions extends MetadataOptions {
    name?: string;
    size?: number;
}
interface MetadataState extends Validation {
    source?: Record<string, unknown>;
    name: string;
    formattedFileName: string;
    abi?: Abi;
}

export { DeriveMetadataOptions as D, MetadataState as M, Validation as V, MetadataOptions as a, MetadataError as b, validateMetadata as v };
