import { ChainId } from './chains.js';
import { M as MetadataState, a as MetadataOptions } from './jsonToAbi-312b7654.js';

interface BasicMetadataFile {
    data: Uint8Array;
    name: string;
    size: number;
}
interface UseMetadata extends MetadataState {
    set: (_: File) => void;
    clear: () => void;
}
declare function useMetadata(options?: MetadataOptions, initialValue?: Record<string, unknown>, chainId?: ChainId): UseMetadata;

export { BasicMetadataFile as B, UseMetadata as U, useMetadata as u };
